<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.makeline">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MakeLine"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAjKMHPVbkVpKiEHwUog0SviRuTvKdloqY" />

        <activity
            android:name=".Map"
            android:label="나만의 원격 줄서기"></activity>
        <activity
            android:name=".Line"
            android:label="나만의 원격 줄서기" />

        <activity
            android:name=".Rank"
            android:label="메뉴를 골라보자" />
        <activity
            android:name=".After"
            android:label="My review" />

        <activity android:name=".MainEventActivity" />
        <activity android:name=".AddNoteActivity" />
        <activity android:name=".NotePreviewActivity" />

        <activity android:name=".MainRank" />


        <activity android:name=".MainPostActivity" />
        <activity android:name=".StartLineActivity" />
        <activity android:name=".NewStoreAct" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>